require 'spec_helper'

describe PictureZoom do
  it 'has a version number' do
    expect(PictureZoom::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
